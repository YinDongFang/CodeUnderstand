[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "taskline"
version = "0.1.0"
description = "A serial, checkpointed async task pipeline library"
requires-python = ">=3.10"

[project.optional-dependencies]
test = ["pytest>=7.0", "pytest-asyncio>=0.23"]

[tool.setuptools]
packages = ["taskline"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
