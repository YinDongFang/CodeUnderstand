"""Stateless helpers unrelated to workflow business rules.

Modules: ``paths`` (task directory layout), ``sandbox`` (workspace path containment),
``lease_util`` (UTC / PID checks), ``log_markers`` (stable log line snippets),
``zip_util`` / ``unzip_util`` (archives).
"""
