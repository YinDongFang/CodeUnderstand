"""Stateless helpers unrelated to workflow business rules.

Modules: ``task_layout`` (task directory layout), ``sandbox`` (workspace containment),
``lease`` (UTC / PID checks), ``log_markers`` (stable log snippets), ``archives`` (zip pack/extract).
"""
