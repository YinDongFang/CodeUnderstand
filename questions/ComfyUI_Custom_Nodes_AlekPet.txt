Wiring this pack into Comfy, translator folders sit next to sketch pose stuff, where in the top init file does the story split between copying web bits and importing python node files?

I cannot find one hand written list of every node class, so how does the loader walk each subfolder and decide which classes actually land in the comfy node map?

The readme install block talks about folder layout under custom_nodes, does anything hardcode the parent folder name or only paths relative to where init py lives?

That big bootstrap in root init mentions requirements and web extension copy, what is the first failure point if pip is slow or the web folder is read only?

In the deep translator folder the json config sits beside the main py file, which globals get filled at import time and what message prints if the json is gone?

Function check_service_view hides paid engines until keys look real, for baidu papago deepl what exact fields must change away from placeholder text?

Proxy lines in deep translator config talk about ip colon port style, where does the code parse that string and what happens when regex says no?

The detect languages json file loads into some global table, in the actual translate call when does it pick single_detection versus batch_detection?

Deep translator prints a message about config js missing even though the path is json, is that string wrong only in the print or also in file open logic?

The google translate py file registers a manual translate route that reads prompt srcTrans toTrans from json body, what response field name does the front end read back?

IDE node save route writes json under the web bundle lib codes area with language subfolders, what json keys must the browser post besides raw code text?

IDEs_DICT keeps per widget ids pointing at small objects, after js completes does wait_js_complete clear flags so the same id can run again clean?

The route ending check_js_complete takes json body, what exact checks must pass before it sets js_complete true and stores js_result?

Load codes get route lists files for one language, if that language folder missing does handler throw or return empty list json?

The exists route path has language in the url but handler reads match_info with wrong key name lang, does exists check always break in real use?

Painter save settings route wants multipart with filename part then data part, it streams chunks to disk with fixed chunk size, why does it only open wb when the json file already existed?

If get_settings_json hits corrupt json it deletes file and recreates empty, can a half crash during write wipe brush data next load?

Loading all painter settings globs json files and strips a fixed suffix from each name field, who consumes that list on the front side?

Remove settings route has a line if not isFileName without parentheses call, does that branch ever run the way author thought?

Pose node builds image dropdown from comfy input directory and may mkdir if missing, how does it behave if input folder has thousands of files first open?

When a multi frame image has frames with different width height the loop does continue skip, does output silently drop frames or error out?

IS_CHANGED hashes whole file bytes for the chosen image name, is that costly if user swaps a huge file while keeping same filename?

When pose mask has no alpha it builds a small zero mask tensor sized sixty four square, does anything upscale that mask before it meets image tensor size?

Preview text node lists hidden prompt and extra png info in input types but the python body ignores them, are those slots only for workflow save metadata?

Hex to hue node calls send_sync with channel name alekpet get color hex plus unique id from hidden input, which front file listens and updates the color picker?

Colors correct node has use_color toggle and optional hex string, if use_color is false does hue degrees input still change pixels or whole branch skips?

Many custom routes share alekpet prefix under PromptServer, is that prefix enough to stay clear of core comfy route names across upgrades?

Deep translator imports a long list of translator classes at top of file, are unused engines still imported even when config turns them off?

Chatglm node imports video download helper from comfy_api_nodes util path, what import error shape do you get on older comfy missing that package?

Root init can fall back without filelock import, if two comfy instances start together and both try dependency install what race can happen?

Dynamic registration uses dir on imported module and keeps callables whose name contains Node substring, can a badly named helper class leak into the menu?

If google env api key is set the code uses translate_by_key http path, does that path honor source language argument same as the free googletrans branch?

Painter helper isFileName uses a weird mix of not and and on empty string, does None filename get treated valid by accident compared to ide node helper?

After you said js completion posts back result_code string, does ide node ever treat empty string as valid completion or it waits forever until timeout?

If deep translator config marks free api true for a service, does check_service_view still demand api key fields before showing that row?

When painter rename settings route runs with overwrite false and target json exists, which branch runs and what json message comes back?

Pose VALIDATE_INPUTS returns a plain string on bad path, does comfy surface that same string in the red error toast or somewhere else?

Does extras preview node OUTPUT_NODE true change whether downstream nodes still receive the string tuple on the same run?

If ide save_code gets filename without extension it adds json suffix, can user accidentally save outside allowed list by odd casing on suffix?

When loading painter all settings hits a broken json mid list loop, does one bad file skip append or abort whole endpoint response?

Cross talk question, does pose node share any http routes with painter node or they stay fully separate under different alekpet paths?

If root init copy of web_alekpet_nodes into comfy web extensions fails halfway, can comfy still load python nodes while front widgets half missing?

Deep translator default api keys dict embeds sample keys for qcri and detect language, are those real rotating keys or just placeholders shipped in repo?

