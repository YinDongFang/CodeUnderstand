Big picture, where does the browser hit first and how does that front file pull in config before anything else runs?

Roughly how do the global config array, the lib side bootstrap, and the main console object depend on each other on each request?

Why ship an old Pheanstalk tree under lib when composer.json also exists, what is the autoload story for this app?

Server rows show up from config file, env var, and a cookie, which source wins when all three have entries?

The UI is mostly php templates under lib tpl plus get params, is there a real router or is it all query string dispatch?

Console init merges three server lists then filters tplMain and tplBlock, which values are actually allowed through for partial page loads?

When action in the url does not match a protected _action method, does the page still render tubes or does it stop quietly?

deleteAllFromTube on delayed checks ready and buried first and posts human errors, what is it trying to prevent before it kicks?

After delete, _postDelete adds five job counters to decide redirect, what happens to the tube param when that sum hits zero?

BeanstalkInterface ctor parses beanstalk url vs host port string, where can a malformed string leave host or port weird?

All peek helpers go through private _peek then _decodeData, which user facing toggles change what you see in the peek panel?

Inside _decodeData the json branch sets content type but comment suggests out might stay raw, what actually gets returned to the template?

If you construct the beanstalk wrapper without passing the settings object, what defaults kick in for json base64 unserialize?

Wrapper kick takes tube and a count, kick job path uses peek by id then client kickJob, when do you pick which path?

Storage class gets a file path string, if create file fails what exception message path do you see in the errors array?

Sample save uses md5 of the display name as storage key, what user visible string comes back when name collides?

readCollection just json_decodes the file, if someone puts invalid json on disk what breaks first in save or getJobs?

Storage layout uses a version key inside the json file, edit sample deletes then saves, why might rollback call saveJob on old copy?

_actionSearch walks three states and builds a job list, how does findJobsByState stop early on time versus count?

Search loop starts from first peeked job id up to a number built from server stats, what id ordering assumption is baked in?

In findJobsByState the break line mixes elapsed seconds with the limit variable from the request, what mistake does that create?

moveJobsFromTo copies body with addJob then deletes old job, does addJob honor priority delay ttr or only raw body?

moveJobsToState only handles ready to buried via reserve zero, why other state pairs bail in the switch default?

Pheanstalk facade constructs a connection with intval on port, what happens if config passes an empty port piece?

Where does a typical command like put or stats flow from facade method into socket write and yaml style parsing?

Pause tube action reads delay from url count param -1 branch, how does Settings getTubePauseSeconds feed the final seconds sent to the server?

Basic auth in the public index compares plain server vars to config password, what happens when auth block missing entirely?

Logout query clears PHP_AUTH user and password in superglobal, does that alone log the user out in most browsers?

Console can run certain actions with no server in the query string, which action names are whitelisted in that early branch?

_actionAddjob posts json back, which post fields map into addJob arguments when some fields left blank strings?

Sample edit posts jobdata through htmlspecialchars_decode before save, where could that differ from raw textarea submit?

getTubeStatValues catches not found server errors and returns empty array, how does the tube stats table behave when that happens?

If you load a saved sample onto a tube, does that path reuse the same addJob signature as the modal add job form?

Cookie beansServers is imploded with semicolons on save, can a stray empty piece show up as a bogus server row?

When delayed bulk delete finds delayed job it kicks a huge number then recurses on ready, why kick instead of delete delayed directly?

New sample and edit sample both scan all configured servers for tube lists, what happens if every connection throws?

Search compares statsJob state string to ready delayed buried, can a reserved job ever match that filter path?

If enableJsonDecode is off in settings, does job search substring still scan the raw string bytes from peek?

After clear tubes posts tube keys, does each tube run delete for all three states in a fixed order every time?

If storage delete returns false always at end of method, does any caller treat return value as success signal?

exceptions_error_handler echoes html during unserialize errors, does that run during normal peek decode or only fatal style paths?

Where does a browser request land first, and which file wires config before the console class boots?

How do the global config array, the lib bootstrap, and the main console object chain together on a normal page load?

Why keep a full Pheanstalk tree under lib if composer is also there, what is the real autoload path the app relies on?

The server dropdown mixes entries from config, an env var, and a cookie, how does that merge behave when the same host appears twice?

Is routing basically query params plus a few tpl switches, or is there another layer hiding behind public index?

In console init, which tplMain and tplBlock strings survive the whitelist before a partial ajax style render?

If action in the url does not map to a real _action method, what does _main still do about tubes and stats?

For delayed bulk delete, why does it peek ready and buried first and bail with those error messages before touching delayed?

After a single delete, code sums five tube counters to decide redirect, when does it drop the tube from the url?

The beanstalk wrapper ctor accepts a url prefix and a plain host port form, where can parsing leave host or port odd?

Peek helpers all funnel through one private helper then decoding, which settings toggles actually change the bytes you see on screen?

Inside job body decoding, base64 and unserialize run before json, what ends up in the returned value if json decode yields an empty array?

If you build the beanstalk wrapper without handing it a settings object, what defaults kick in for json and friends?

Kick by tube count versus kick single job by id, which user action maps to which code path on the server?

The storage class takes a file path from config, if the file cannot be created what exception text bubbles to the user?

Saving a sample hashes the name for the key, what message comes back when that name already exists in the json file?

readCollection blindly json_decodes disk contents, what is the first place that explodes if the json file is corrupt?

Edit sample deletes the old key then save may fail, how does the rollback path put the previous record back?

The search action loops three job states, how does the inner finder decide to stop on wall clock versus match count?

Search walks numeric job ids up to a bound built from server stats counters, what ordering assumption does that rely on?

The search loop mixes elapsed seconds with the limit from the query string in one condition, what weird early exit can that cause?

Moving jobs to another tube copies body with addJob then deletes the old job, does that path set priority delay ttr or only payload?

State move from ready to buried uses reserve with zero timeout, why does the code refuse other source states in the default branch?

The pheanstalk facade int casts the port argument, what happens when the port piece in the string is missing or empty?

For a normal put or stats call, what is the short path from the facade method down to the socket write and response parse?

Pause uses the url count param sometimes -1, then settings pick seconds, how does that number reach pauseTube on the client?

When auth is disabled in config, which branch skips sending the 401 headers and still loads the console?

Logout clears PHP auth fields in the super global, does that really end basic auth sessions in common browsers?

Some actions run with no server query param, which action names are allowed in that early return branch?

The ajax add job endpoint returns json, which post fields map into addJob when priority delay ttr arrive empty?

Sample edit posts job data through htmlspecialchars decode before save, when does that differ from what the textarea actually held?

Tube stats fetch catches not found from the server and returns an empty array, how does the tube page render when that happens?

Loading a saved sample onto a tube calls addJob, does that reuse the same argument pattern as the modal add job path?

Servers saved in the cookie join with semicolons, can a blank chunk show up as a fake server entry?

Delayed bulk delete kicks a very large count then recurses on ready, why not delete delayed directly in one step?

New sample and edit sample both scan every server for tubes, what does the page do if every connection attempt fails?

Search filters by stats job state string against ready delayed buried, can a reserved job ever enter that matching path?

If json decode is turned off in settings, does substring search still see the raw string from peek unchanged?

Clear tubes loops posted tube names then three states each, is the order of states fixed every time?

The storage delete method always returns false at the end even when it removed something, does any caller depend on that return?

The custom error handler prints red html during bad unserialize or base64, does normal json off peek still hit that handler?

Config local merge checks nested array count with a magic range, what breaks if your local file is missing new keys?

Session name is set before templates run, which cookie name ties browser tabs to the flash style info message path?

Reloader action switches tpl to ajax allTubes, what user visible refresh path hits that compared to full page reload?

