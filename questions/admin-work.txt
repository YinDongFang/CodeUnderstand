When I open the app, what order stuff get wired in that boot function, pinia first or router first.

This app uses hash router not history style, any practical reason you see in this repo.

After login, where those backend menu routes finally land inside vue router.

Pinia and router guards feel glued together, if I only got ten minutes which area I read first.

That mock import at startup, what it really does besides side effect loading.

Permission guard has a white list path list, say exactly which paths skip the token check.

permissionRoutes empty then init runs and navigation returns same target with replace true, I don't get why replace not a normal revisit.

Visited guard skips some pages by checking route name strings like 404 and Login, are those names guaranteed by route definitions or kinda fragile.

Cached name list starts zero, initCachedRoute scans router.getRoutes, walk me when that fires relative to other guards.

Four files register beforeEach, any rule which listener runs first in practice.

Shared http helper, GET puts data into axios params and POST sends same object as body, if I pass data as a function what happens.

successHandler inside http only accepts response code 200, other code throws, who catches that message next in the login click flow.

Axios response error path rejects a plain object not a real AxiosError, downstream code still happy or any typing surprise.

Request interceptor defaults json content type, if header says form urlencoded it runs qs stringify on data, point me the branch.

CustomRequestInterceptor sets Auth header from store token, if Auth already set manually then what wins.

Token expire check is really just empty token string, no timestamp parsing anywhere in user store.

User store persist excludes userName field, after hard refresh which fields still come back from local storage.

logout clears local and session storage then resets store, anything reset dynamic routes in the same call.

Some code uses that context helper for user store, some imports default store, new code I add should follow which pattern.

saveUser wraps writes in Promise then resolve, login flow waits that resolve before navigation or not really.

generatorRoutes merges server menu with local async routes when match, else builds leaf route using getComponent, what path string it glues for the vue file lookup.

mapTwoLevelRouter flattens nested children paths, what layout behavior needs that shape.

findRootPathRoute looks for meta isRootPath on a child, if I forget it what default redirect happens on slash route.

import.meta.glob loads vue files under views, backend menu path wrong, what runtime pain I should expect.

outLink external sets route path to full http url, sidebar menu treats external different from internal how.

Login success replaces to redirect query if present else home, if routes still empty at that moment can it bounce back to login.

Main keep-alive include reads cached route name array from store, where names tie to route meta cacheable stuff.

Main wraps page with component key tied to fullPath, what bug that key tries to avoid when switching tabs.

Login posts relative login path plus axios baseURL, simple picture of final request url string.

Permission init posts roleId and userId from user store into menu endpoint, which login response fields fill those ids.

Between built-in axios request interceptor and CustomRequestInterceptor, who runs last for Content-Type vs Auth header.

Dynamic routes added then catch all 404 route added last on purpose, if dev adds another wild route earlier what breaks first.

Interceptor reads token from pinia singleton file, first request right after refresh still safe or any race with rehydrate.

Permission guard checks path string white list, visited guard checks route name strings, any weird half open page edge case from mismatch.

When menu endpoint constant goes falsy branch it still calls generatorRoutes with default route tree, where that default route definition lives in repo roughly.

getRoutes catches failure and returns empty array, init still adds slash redirect from findRootPathRoute on empty list, what page user probably lands on.

Persist plugin restores user json before router kicks in, first permission guard sees token or still empty for a moment.

Http post path always axios.post even if caller passes another method string in options, PUT ever reachable here.

That hooks file under hooks folder named like get helper but export function name looks copied from post helper, if thats a typo how bad for callers.

